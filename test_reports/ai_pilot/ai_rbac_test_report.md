# OCB TITAN ERP - AI RBAC Test Report
**Generated:** 2026-03-14
**Target Tenant:** ocb_titan (PILOT ONLY)
**AI Version:** 1.0.0

---

## EXECUTIVE SUMMARY

| Test | Status |
|------|--------|
| RBAC enforcement on AI endpoints | ✅ PASS |
| Unauthorized roles blocked | ✅ PASS |
| Module-level permissions | ✅ PASS |
| Admin-only config access | ✅ PASS |

---

## ALLOWED ROLES FOR AI

| Role | Code | AI Access |
|------|------|-----------|
| Owner | owner | ✅ Full |
| Super Admin | super_admin | ✅ Full |
| Auditor | auditor | ✅ Limited |
| Manager | manager | ✅ Limited |

---

## DENIED ROLES FOR AI

| Role | Code | AI Access | Response |
|------|------|-----------|----------|
| Cashier | cashier | ⛔ Denied | 403 |
| Warehouse | warehouse | ⛔ Denied | 403 |
| Finance | finance | ⛔ Denied | 403 |

---

## MODULE PERMISSIONS BY ROLE

| Role | Sales | Inventory | Finance | CEO | Logs | Config |
|------|-------|-----------|---------|-----|------|--------|
| owner | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| super_admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| auditor | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ |
| manager | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |

---

## RBAC IMPLEMENTATION

### File: `/app/backend/ai_service/rbac_gateway.py`

```python
AI_ALLOWED_ROLES = ["owner", "super_admin", "auditor", "manager"]

AI_ROLE_PERMISSIONS = {
    "owner": ["sales", "inventory", "finance", "ceo", "logs", "config"],
    "super_admin": ["sales", "inventory", "finance", "ceo", "logs", "config"],
    "auditor": ["sales", "inventory", "finance", "logs"],
    "manager": ["sales", "inventory", "ceo"]
}

class AIRBACGateway:
    @staticmethod
    def check_access(user: dict, module: str = None) -> bool:
        role = user.get("role_code") or user.get("role") or ""
        role = role.lower()
        
        if role not in AI_ALLOWED_ROLES:
            raise HTTPException(
                status_code=403,
                detail=f"AI Engine access denied. Role '{role}' tidak memiliki akses."
            )
        
        if module:
            allowed_modules = AI_ROLE_PERMISSIONS.get(role, [])
            if module not in allowed_modules:
                raise HTTPException(
                    status_code=403,
                    detail=f"AI module '{module}' tidak diizinkan untuk role '{role}'"
                )
        
        return True
```

---

## TEST RESULTS

### Test 1: Owner Access (Full)
- **Role:** owner
- **Endpoints Tested:** All AI endpoints
- **Result:** ✅ ALL ALLOWED

### Test 2: Auditor Access (Limited)
- **Role:** auditor
- **Allowed:** sales, inventory, finance, logs
- **Denied:** ceo, config
- **Result:** ✅ CORRECTLY LIMITED

### Test 3: Manager Access (Limited)
- **Role:** manager
- **Allowed:** sales, inventory, ceo
- **Denied:** finance, logs, config
- **Result:** ✅ CORRECTLY LIMITED

### Test 4: Cashier Access (Denied)
- **Role:** cashier
- **Expected:** 403 Forbidden
- **Result:** ✅ CORRECTLY DENIED

### Test 5: Unauthenticated Access
- **Token:** None
- **Expected:** 401/403
- **Result:** ✅ BLOCKED

---

## ERROR RESPONSES

### Unauthorized Role
```json
{
    "detail": "AI Engine access denied. Role 'cashier' tidak memiliki akses."
}
```

### Module Not Allowed
```json
{
    "detail": "AI module 'finance' tidak diizinkan untuk role 'manager'"
}
```

---

## ENDPOINT PROTECTION

```python
# Example: Sales Insights Endpoint
@router.get("/sales/insights")
async def get_sales_insights(user: dict = Depends(get_current_user)):
    AIKillSwitch.check_or_raise()  # Kill switch check
    AIRBACGateway.check_access(user, "sales")  # RBAC check
    # ... rest of implementation
```

---

## CONCLUSION

**AI RBAC TEST: PASSED ✅**

- All AI endpoints are protected by RBAC
- Only authorized roles can access AI
- Module-level permissions enforced
- Error messages are informative but secure

---

*Evidence generated by OCB TITAN AI Security Audit*
